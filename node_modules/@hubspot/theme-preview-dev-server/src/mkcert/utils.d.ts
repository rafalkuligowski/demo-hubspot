export { existsSync } from "fs";
export { readFile } from "fs/promises";
export function getDefaultHosts(): string[];
export function exec(cmd: string, options: import("child_process").ExecOptions): Promise<{
    stdout: string;
    stderr: string;
}>;
export const DATA_DIR: string;
export function resolvePath(path: string, baseDir?: string | undefined): string;
export function wrapInQuotes(path?: string | undefined): `"${string}"`;
export function writeFile(filePath: string, data: string | Uint8Array): Promise<void>;
export function ensureDirExist(filePath: string): Promise<void>;
export function getAbsolutePath(relative: string, filePath: string): string;
