export function createCertificate(mkcertOptions: {
    force?: boolean;
    autoUpgrade?: boolean;
    keyFilePath: string;
    certFilePath: string;
}, hosts?: string[] | undefined): Promise<{
    key: Buffer;
    cert: Buffer;
}>;
